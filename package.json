{
  "name": "mealmint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "recipes:extract": "python3 scripts/extract_sources.py",
    "recipes:build": "python3 scripts/build_catalogue.py"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
